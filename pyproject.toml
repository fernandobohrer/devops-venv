[project]
name = ""
version = "0.0.1"
dependencies = [

  # Dependencies with set versions - pinned for stability and compatibility:

  "ansible          ==  12.0.0",
  "ansible-lint     ==  25.9.0",
  "ansible-creator  ==  25.9.0",
  "mitogen          ==  0.3.29",
  "pre-commit       ==   4.3.0",
  "yamllint         ==  1.37.1",

  # Dependencies set to use the latest version - to allow easy access to new features:

  "boto3",

  # Dependencies pinned to older versions due to compatibility issues with other packages:
  # https://github.com/ansible-community/molecule-plugins/issues/301#issuecomment-2629683469

  "molecule                   ==  25.1.0",
  "molecule-plugins[ec2]      ==  23.7.0",
  "molecule-plugins[docker]   ==  23.7.0",
  "molecule-plugins[vagrant]  ==  23.7.0",
]

[dependency-groups]
dev = []

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
